Toast messages can be displayed by importing the static `notifications` class then calling the `toast()` method.

```typescript
import notifications from "~brixi/controllers/alerts";

notifications.toast(`Toast message example.`);
```
